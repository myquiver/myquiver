--source ../include/my-quiver/have-my-quiver.inc
--source ../include/my-quiver/have-my-quiver-helper.inc
--disable_warnings
DROP TABLE IF EXISTS example;
--enable_warnings
--copy_file  suite/my-quiver/example.parquet $MYSQLD_DATADIR/test/example.parquet

CREATE TABLE example (
  id int
);

SELECT * FROM example;

DROP TABLE example;
--remove_file $MYSQLD_DATADIR/test/example.parquet

--source ../include/my-quiver/have-my-quiver-deinit.inc
